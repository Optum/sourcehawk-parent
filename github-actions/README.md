# Sourcehawk Shared Github Actions

These actions can be used like so:

```yml
- name: Run Action
  uses: optum/sourcehawk-parent/github-actions/example@main
```

In the above example, `example` is the name of the action